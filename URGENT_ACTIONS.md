# 🚨 СРОЧНЫЕ ДЕЙСТВИЯ ПО БЕЗОПАСНОСТИ

## ⚠️ КРИТИЧНО: Выполните эти шаги НЕМЕДЛЕННО!

### 0️⃣ Удалить релиз v4.0.5 (СНАЧАЛА!)

```bash
# Релиз v4.0.5 содержит скомпрометированные .env и config.json!
gh release delete v4.0.5 --yes

# Удалить тег локально и на GitHub
git tag -d v4.0.5
git push origin :refs/tags/v4.0.5
```

**ПОЧЕМУ:** Релиз содержит исходный код с .env и config.json. Все кто скачает Source code.zip получат ваши секреты!

---

### 1️⃣ Force Push на GitHub (ОБЯЗАТЕЛЬНО!)

```bash
cd /Users/olgazaharova/Project/ProjectPython/VPNserverManage-Clean

# Перезаписать историю на GitHub
git push origin --force --all
git push origin --force --tags
```

**ВНИМАНИЕ:** Это перезапишет всю историю на GitHub!

---

### 2️⃣ Проверить удаление файлов

После push проверьте, что файлы удалены:

```bash
# Должны вернуть ошибку "Not Found"
gh api repos/:owner/:repo/contents/.env 2>&1 | grep "Not Found"
gh api repos/:owner/:repo/contents/config.json 2>&1 | grep "Not Found"
```

Или откройте в браузере (должна быть ошибка 404):
- https://github.com/kureinmaxim/vpn-server-manager/blob/main/.env
- https://github.com/kureinmaxim/vpn-server-manager/blob/main/config.json

---

### 3️⃣ Изменить PIN-код

Старый PIN `1234` был скомпрометирован!

1. Откройте `config.json`
2. Измените `default_pin` и `current_pin` на новый
3. Или измените через интерфейс приложения

---

### 4️⃣ Проверить SECRET_KEY

```bash
# Убедитесь что .env существует и содержит новый ключ
cat .env | grep SECRET_KEY
```

Если `.env` не существует:
```bash
python3 generate_key.py
```

---

## ✅ Что уже сделано:

- ✅ `.env` удален из истории Git (локально)
- ✅ `config.json` удален из истории Git (локально)
- ✅ `data/*.enc` удалены из истории Git (локально)
- ✅ Сгенерирован новый SECRET_KEY
- ✅ Обновлен .gitignore
- ✅ Созданы документы по безопасности

---

## ❌ Что НЕ сделано (требует ваших действий):

- ❌ **УДАЛИТЬ РЕЛИЗ v4.0.5** (содержит секреты!)
- ❌ Force push на GitHub
- ❌ Проверка удаления с GitHub
- ❌ Изменение PIN-кода
- ❌ Пересоздать релиз v4.0.5 (после force push)

---

## 🔄 ДОПОЛНИТЕЛЬНО: Пересоздание релиза v4.0.5

После выполнения шагов 0-3, пересоздайте релиз:

```bash
# Убедитесь, что вы на последнем коммите
git pull origin main

# Создать новый релиз (DMG нужно будет пересобрать)
gh release create v4.0.5 \
  --title "v4.0.5 - Bug Fixes and Security Updates" \
  --notes "
## 🐛 Исправления
- Функция 'Сохранить как PNG' полностью реализована
- PIN аутентификация: исправлено отображение ошибок
- SSH подключение: поддержка нестандартных портов

## 🔒 Безопасность
- **ВАЖНО:** Исправлены критические проблемы безопасности
- Конфиденциальные файлы удалены из репозитория
- DMG больше не содержит .env и config.json

Полный список изменений в CHANGELOG.md
"

# Загрузите новый DMG после пересборки
# gh release upload v4.0.5 VPNServerManager-Clean_Installer.dmg
```

---

## 📚 Подробная информация:

- [SECURITY_AUDIT_REPORT.md](./SECURITY_AUDIT_REPORT.md) - Полный отчет
- [SECURITY.md](./SECURITY.md) - Руководство по безопасности

---

**Время выполнения:** ~5 минут  
**Приоритет:** КРИТИЧЕСКИЙ
