# Руководство по миграции

## 🚀 Быстрая миграция

### 1. Создайте резервную копию
```bash
cp -r data data_backup
cp config.json config.json.backup
```

### 2. Установите новые зависимости
```bash
pip install -r requirements.txt
```

### 3. Настройте окружение
```bash
cp env.example .env
# Отредактируйте .env файл
```

### 4. Запустите новое приложение
```bash
# Web режим
python run.py

# Desktop режим  
python run.py --desktop
```

## 📋 Что изменилось

### Структура файлов
- **Было**: `app.py` (монолит)
- **Стало**: `app/` (модульная структура)

### Запуск приложения
- **Было**: `python app.py`
- **Стало**: `python run.py`

### Конфигурация
- **Было**: `config.json`
- **Стало**: `.env` файл

### Режимы запуска
- **Web**: `python run.py`
- **Desktop**: `python run.py --desktop`
- **Debug**: `python run.py --debug`

## 🔧 Настройка

### Переменные окружения (.env)
```env
SECRET_KEY=your-secret-key-here
BABEL_DEFAULT_LOCALE=ru
DATA_DIR=data
SERVERS_FILE=servers.json.enc
DEFAULT_PIN=1234
```

### Команды разработки
```bash
make install-dev    # Установка для разработки
make test          # Запуск тестов
make lint          # Проверка кода
make format        # Форматирование
```

## ⚠️ Важные замечания

1. **Данные**: Формат данных остался совместимым
2. **UI**: Интерфейс не изменился
3. **Функции**: Вся функциональность сохранена
4. **API**: Endpoints остались теми же

## 🆘 Решение проблем

### Ошибка импорта
```bash
# Убедитесь, что установлены все зависимости
pip install -r requirements.txt
```

### Ошибка конфигурации
```bash
# Проверьте .env файл
cp env.example .env
```

### Ошибка запуска
```bash
# Проверьте логи
tail -f logs/app.log
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в `logs/app.log`
2. Убедитесь в правильности `.env` файла
3. Запустите тесты: `make test`
4. Создайте issue в репозитории
