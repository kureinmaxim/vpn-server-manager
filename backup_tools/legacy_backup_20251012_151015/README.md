# Legacy Files Backup

**Дата архивирования**: 12 октября 2025, 15:10

## 📦 Содержимое

Эта папка содержит устаревшие файлы от **монолитной версии** приложения (до v4.0):

### Файлы:

1. **app.py** (2899 строк)
   - Старый монолитный код всего приложения
   - Содержал весь функционал в одном файле
   - Заменён модульной структурой в папке `app/`

2. **pin_auth.py** (307 строк)
   - Старая реализация PIN-аутентификации
   - Использовал класс `PinAuth` с отдельным файлом состояния
   - Заменён встроенной логикой в `app/routes/api.py`

3. **test_basic.py** (6.1 KB)
   - Устаревшие тесты для старой монолитной версии
   - Заменён современными тестами в папке `tests/`

## 🔄 Текущая структура (v4.0+)

### Новая архитектура:
- ✅ **Модульная структура** в `app/`
  - `app/__init__.py` - Application Factory
  - `app/routes/` - Blueprints (main.py, api.py)
  - `app/services/` - Бизнес-логика (ssh, crypto, data_manager, api)
  - `app/models/` - Модели данных
  - `app/utils/` - Утилиты и декораторы

- ✅ **Точки входа**:
  - `run.py` - Основная точка входа (web/desktop режимы)
  - `launch_gui.py` - Desktop GUI с pywebview
  - `run_desktop.py` - Legacy launcher

- ✅ **PIN-аутентификация**:
  - Встроена в `app/routes/api.py`
  - Использует Flask session
  - Логика блокировки в сессии

### Сборка:
- `make dist` → `python build_macos.py` → `VPNServerManager-Clean.spec` → `launch_gui.py`
- Не использует старые `app.py` и `pin_auth.py`

## 🚫 Почему удалены?

1. **Не используются в production**:
   - `Makefile` использует `run.py`
   - PyInstaller spec использует `launch_gui.py`
   - Модульная структура в `app/` полностью заменила монолит

2. **Могут вызывать путаницу**:
   - Разработчики могут случайно запустить старую версию
   - Старая логика PIN-аутентификации конфликтует с новой
   - Дублирование кода усложняет поддержку

3. **Безопасность**:
   - Старая версия может иметь неисправленные уязвимости
   - Устаревшая логика блокировки PIN

## 📝 История миграции

- **v1.0 - v3.x**: Монолитная архитектура (`app.py` + `pin_auth.py`)
- **v4.0+**: Модульная архитектура (Application Factory + Blueprints)
- **v4.0.3** (текущая): Полная миграция завершена, legacy код удалён

## 🔙 Восстановление (если нужно)

Если по какой-то причине нужно восстановить старые файлы:

```bash
cd /Users/olgazaharova/Project/ProjectPython/VPNserverManage-Clean
cp backup_tools/legacy_backup_20251012_151015/app.py .
cp backup_tools/legacy_backup_20251012_151015/pin_auth.py .
cp backup_tools/legacy_backup_20251012_151015/test_basic.py .
```

⚠️ **Внимание**: Восстановление может нарушить работу текущей версии!

## 📚 Документация

- **Архитектура**: `ArchitecturalRules_v403.md`
- **Новая структура**: `README_NEW_STRUCTURE.md`
- **История изменений**: `CHANGELOG.md`
- **Тестирование**: `Test_and_Restructuring/RESTRUCTURING_REPORT.md`

---

**Создано автоматически при рефакторинге**

