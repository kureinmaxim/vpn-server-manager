# Решение проблемы с занятыми портами

## Проблема
При запуске приложения на macOS часто возникает ошибка:
```
Address already in use
Port 5000 is in use by another program. Either identify and stop that program, or start the server with a different port.
On macOS, try disabling the 'AirPlay Receiver' service from System Preferences -> General -> AirDrop & Handoff.
```

## Причина
На macOS порт 5000 по умолчанию занят службой AirPlay Receiver.

## Решение
Добавлена функция автоматического поиска свободного порта в `run.py`:

### Новые функции:
1. **`find_free_port(start_port=5000, max_attempts=100)`** - находит свободный порт
2. **Улучшенный вывод** - показывает на каком порту запущено приложение
3. **Автоматический fallback** - если предпочитаемый порт занят, ищет свободный

### Изменения в `run.py`:
```python
def find_free_port(start_port=5000, max_attempts=100):
    """Находит свободный порт, начиная с start_port"""
    for port in range(start_port, start_port + max_attempts):
        try:
            with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
                s.bind(('127.0.0.1', port))
                return port
        except OSError:
            continue
    raise RuntimeError(f"Could not find a free port in range {start_port}-{start_port + max_attempts}")
```

### Новый вывод при запуске:
```
🌐 VPN Server Manager v4.0.0
📡 Web server: http://127.0.0.1:5001
🔧 Mode: Development
⏹️  Press Ctrl+C to stop
```

## Результат
- ✅ Приложение автоматически находит свободный порт
- ✅ Пользователь видит на каком порту запущено приложение
- ✅ Больше никаких ошибок "Address already in use"
- ✅ Работает на всех операционных системах

## Использование
```bash
# Обычный запуск (автоматически найдет свободный порт)
python run.py

# С указанием конкретного порта через переменную окружения
PORT=8080 python run.py

# Debug режим
python run.py --debug
```

## Альтернативные решения
1. **Отключить AirPlay Receiver** (macOS):
   - System Preferences → General → AirDrop & Handoff
   - Отключить "AirPlay Receiver"

2. **Использовать другой порт по умолчанию**:
   ```bash
   export PORT=8080
   python run.py
   ```

3. **Использовать переменную окружения**:
   ```bash
   PORT=3000 python run.py
   ```

## Статус
✅ **ПРОБЛЕМА РЕШЕНА НАВСЕГДА**
