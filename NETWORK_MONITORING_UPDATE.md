# Обновление системы мониторинга сетевого трафика

## Дата: 15 октября 2025

## ✅ Исправленные проблемы

### 1. Заниженный учет сетевого трафика

**Проблема:**
Мониторинг показывал заниженные значения трафика при использовании VPN, так как учитывался только один сетевой интерфейс (обычно `eth0`), а VPN-трафик проходил через отдельные виртуальные интерфейсы (`tun0`, `wg0`, `tap0`).

**Решение:**
- ✅ Теперь мониторинг автоматически находит и учитывает **все активные сетевые интерфейсы**
- ✅ Поддержка физических интерфейсов: `eth0`, `ens3`, `wlan0` и т.д.
- ✅ Поддержка VPN-интерфейсов: `tun0`, `tap0`, `wg0`, `ppp0`, `ipsec0`
- ✅ Трафик суммируется со всех интерфейсов для точного учета
- ✅ Отображается список мониторимых интерфейсов в интерфейсе

**Пример:**
```
Было:
↓ Загрузка: 0.05 MB/s (только eth0)

Стало:
🔍 Мониторинг: eth0, tun0, wg0 (3 интерфейсов)
↓ Загрузка: 15.42 MB/s (весь трафик)
```

**Измененные файлы:**
- `app/services/ssh_service.py` - метод `get_network_stats()`
- `templates/monitoring.html` - отображение списка интерфейсов

---

### 2. Надоедливые диалоги "Уйти/Остаться"

**Проблема:**
При переходе между вкладками внутри приложения (например, с главной страницы на настройки) появлялось надоедливое системное сообщение "Вы уверены, что хотите покинуть страницу?".

**Решение:**
- ✅ Добавлен умный обработчик, который различает:
  - **Внутренние переходы** (между страницами приложения) → предупреждение НЕ показывается
  - **Закрытие вкладки/браузера** → предупреждение показывается (защита от случайного закрытия)
  - **Переход на внешний сайт** → предупреждение показывается

**Как работает:**
1. Система отслеживает клики по ссылкам
2. Проверяет, является ли ссылка внутренней (тот же домен)
3. Для внутренних переходов отключает предупреждение
4. Для закрытия вкладки/браузера оставляет предупреждение

**Измененные файлы:**
- `templates/layout.html` - логика обработки `beforeunload`

---

## 🎯 Результат

### Что улучшилось:

1. **Точный мониторинг трафика**
   - Видите реальное потребление трафика, включая VPN
   - Понимаете, какие интерфейсы активны на сервере
   - Корректная суточная статистика

2. **Комфортная навигация**
   - Свободно переключайтесь между вкладками без надоедливых предупреждений
   - Защита от случайного закрытия приложения сохранена

3. **Прозрачность**
   - Видите список всех мониторимых интерфейсов
   - Понимаете, откуда берутся данные

---

## 📋 Для проверки

### Тест 1: Мониторинг трафика

1. Откройте мониторинг любого сервера
2. Проверьте, что отображается строка: `🔍 Мониторинг: eth0, tun0, ...`
3. Подключитесь к серверу через VPN и начните скачивать что-то
4. Значения трафика должны корректно отображаться

### Тест 2: Навигация

1. Откройте приложение и войдите с PIN
2. Переходите между вкладками: Главная → Настройки → Мониторинг → Помощь
3. Предупреждения "Уйти/Остаться" **НЕ должны** появляться
4. Попробуйте закрыть вкладку браузера → предупреждение **должно** появиться

---

## 🔧 Технические детали

### Мониторинг интерфейсов

**Команда для обнаружения:**
```bash
ls /sys/class/net/ | grep -v '^lo$' | grep -E '^(eth|ens|eno|enp|wlan|wlp|tun|tap|wg|ppp|ipsec)'
```

**Поддерживаемые типы:**
- Ethernet: `eth0`, `ens3`, `eno1`, `enp0s3`
- Wi-Fi: `wlan0`, `wlp3s0`
- OpenVPN: `tun0`, `tap0`
- WireGuard: `wg0`
- PPTP/L2TP: `ppp0`
- IPSec: `ipsec0`

### Алгоритм суммирования трафика

```python
# Для каждого интерфейса
for iface in interfaces:
    # Читаем rx_bytes (входящий) и tx_bytes (исходящий)
    rx_bytes = read("/sys/class/net/{iface}/statistics/rx_bytes")
    tx_bytes = read("/sys/class/net/{iface}/statistics/tx_bytes")
    
    # Суммируем
    total_rx += rx_bytes
    total_tx += tx_bytes

# Вычисляем скорость (разница за 1 секунду)
rx_speed = (total_rx2 - total_rx1) / time_diff / 1048576  # MB/s
```

---

## 📚 Дополнительная документация

Подробная документация доступна в файле:
- `docs/NETWORK_MONITORING_FIX.md`

---

## ⚠️ Важно

### Для серверов без VPN:
- Мониторинг будет работать как раньше (только физические интерфейсы)
- Никаких изменений в конфигурации не требуется

### Для серверов с VPN:
- VPN-интерфейсы автоматически обнаруживаются
- Трафик учитывается автоматически
- Установка дополнительных пакетов не требуется

---

## 🎉 Готово к использованию!

Перезапустите приложение и проверьте обновления:

**Windows:**
```cmd
venv\Scripts\python.exe run.py
```

**macOS/Linux:**
```bash
source venv/bin/activate && python3 run.py
```

Откройте браузер: http://127.0.0.1:5000

---

**Версия:** 4.0.10  
**Дата обновления:** 15 октября 2025  
**Статус:** ✅ Протестировано и готово к использованию

