# 🔐 Руководство по контрольным суммам SHA-256

## 📋 Содержание

- [Что такое контрольная сумма?](#что-такое-контрольная-сумма)
- [Зачем это нужно?](#зачем-это-нужно)
- [Как создать контрольную сумму](#как-создать-контрольную-сумму)
- [Как проверить контрольную сумму](#как-проверить-контрольную-сумму)
- [Примеры использования](#примеры-использования)
- [Автоматизация](#автоматизация)

---

## 🔍 Что такое контрольная сумма?

**Контрольная сумма (checksum)** — это уникальная строка символов, которая генерируется из файла с помощью криптографического алгоритма.

**SHA-256** — это алгоритм хеширования, который создает 256-битный (64 символа в hex) уникальный отпечаток файла.

### Пример:

```
Файл: VPN-Server-Manager-Setup-v4.0.7.exe
SHA-256: 5CCD6B7C30F34C8715BFA54561F9E2A0B6B90EB3AC6B4AB491CCAB0396989B11
```

### Свойства SHA-256:

✅ **Детерминированность** — один и тот же файл всегда дает одну и ту же сумму  
✅ **Уникальность** — разные файлы (почти) никогда не дают одинаковую сумму  
✅ **Односторонность** — невозможно восстановить файл из контрольной суммы  
✅ **Чувствительность** — даже изменение 1 байта полностью меняет сумму

---

## 🎯 Зачем это нужно?

### 1. **Проверка целостности файла**

Убедиться, что файл не поврежден при загрузке или передаче:

```
Ситуация: Файл скачался не полностью
Результат: SHA-256 не совпадет с оригиналом
```

### 2. **Защита от подмены**

Обнаружить, если файл был изменен злоумышленниками:

```
Ситуация: Кто-то внедрил вирус в инсталлятор
Результат: SHA-256 изменится, пользователь заметит подделку
```

### 3. **Доверие к источнику**

Подтвердить, что файл действительно от разработчика:

```
Ситуация: Скачали с неофициального сайта
Действие: Сравнить SHA-256 с официальной документацией
```

### 4. **Юридическая защита**

Доказать, что распространялась конкретная версия:

```
Ситуация: Претензия о вредоносном ПО
Защита: Контрольная сумма подтверждает оригинальную версию
```

---

## 🛠️ Как создать контрольную сумму

### Вариант 1: PowerShell (рекомендуется)

```powershell
# Простой вывод
Get-FileHash "VPN-Server-Manager-Setup-v4.0.7.exe" -Algorithm SHA256

# Сохранить в файл
Get-FileHash "VPN-Server-Manager-Setup-v4.0.7.exe" -Algorithm SHA256 | Select-Object -ExpandProperty Hash | Out-File -FilePath "checksum.txt" -Encoding ASCII

# Красивый вывод
Get-FileHash "VPN-Server-Manager-Setup-v4.0.7.exe" -Algorithm SHA256 | Format-List
```

**Результат:**
```
Algorithm : SHA256
Hash      : 5CCD6B7C30F34C8715BFA54561F9E2A0B6B90EB3AC6B4AB491CCAB0396989B11
Path      : C:\...\VPN-Server-Manager-Setup-v4.0.7.exe
```

### Вариант 2: Command Prompt (CMD)

```cmd
certutil -hashfile "VPN-Server-Manager-Setup-v4.0.7.exe" SHA256

REM Сохранить в файл
certutil -hashfile "VPN-Server-Manager-Setup-v4.0.7.exe" SHA256 > checksum.txt
```

**Результат:**
```
SHA256 hash of VPN-Server-Manager-Setup-v4.0.7.exe:
5ccd6b7c30f34c8715bfa54561f9e2a0b6b90eb3ac6b4ab491ccab0396989b11
CertUtil: -hashfile command completed successfully.
```

### Вариант 3: GUI инструменты

**HashTab** (расширение для Windows Explorer):
1. Скачать с [implbits.com](http://implbits.com/products/hashtab/)
2. Установить
3. Правый клик на файл → Свойства → Вкладка "File Hashes"

---

## ✅ Как проверить контрольную сумму

### Способ 0: Готовые скрипты (самый простой)

Для вашего удобства мы создали готовые скрипты проверки:

**PowerShell скрипт (`verify_installer.ps1`):**
```powershell
.\verify_installer.ps1
```

**Batch скрипт (`verify_installer.bat`):**
```cmd
verify_installer.bat
```

Эти скрипты автоматически:
- ✅ Проверят наличие файлов
- ✅ Прочитают официальную контрольную сумму из `checksum.txt`
- ✅ Вычислят контрольную сумму инсталлятора
- ✅ Сравнят суммы и покажут результат
- ✅ Предложат запустить установку, если проверка пройдена

**Примечание:** Скрипты должны находиться в одной папке с инсталлятором и `checksum.txt`.

---

### Способ 1: Автоматическая проверка (PowerShell)

Создайте скрипт `verify_checksum.ps1`:

```powershell
# Параметры
$File = "VPN-Server-Manager-Setup-v4.0.7.exe"
$ExpectedHash = "5CCD6B7C30F34C8715BFA54561F9E2A0B6B90EB3AC6B4AB491CCAB0396989B11"

# Вычисляем текущую сумму
$ActualHash = (Get-FileHash $File -Algorithm SHA256).Hash

# Сравниваем
if ($ActualHash -eq $ExpectedHash) {
    Write-Host "✓ ПРОВЕРКА ПРОЙДЕНА!" -ForegroundColor Green
    Write-Host "Файл подлинный и не поврежден." -ForegroundColor Green
} else {
    Write-Host "✗ ПРОВЕРКА НЕ ПРОЙДЕНА!" -ForegroundColor Red
    Write-Host "ВНИМАНИЕ: Файл поврежден или подделан!" -ForegroundColor Red
    Write-Host ""
    Write-Host "Ожидалось: $ExpectedHash" -ForegroundColor Yellow
    Write-Host "Получено:  $ActualHash" -ForegroundColor Yellow
}
```

**Запуск:**
```powershell
.\verify_checksum.ps1
```

### Способ 2: Ручная проверка

1. **Получить сумму файла:**
   ```powershell
   Get-FileHash "VPN-Server-Manager-Setup-v4.0.7.exe" -Algorithm SHA256
   ```

2. **Открыть `checksum.txt`** (или README с официальной суммой)

3. **Сравнить символ за символом**

**Важно:** Регистр не важен (`5CCD...` = `5ccd...`)

### Способ 3: Через CMD

```cmd
certutil -hashfile "VPN-Server-Manager-Setup-v4.0.7.exe" SHA256 | findstr /v "hash CertUtil"
```

Сравнить вывод с `checksum.txt`.

### Способ 4: Быстрая проверка (одна команда PowerShell)

Скопируйте и вставьте в PowerShell:

```powershell
$Expected = Get-Content "checksum.txt" -Raw | ForEach-Object {$_.Trim()}
$Actual = (Get-FileHash "VPN-Server-Manager-Setup-v4.0.7.exe" -Algorithm SHA256).Hash
if ($Expected -eq $Actual) {
    Write-Host "✓ OK: Файл подлинный!" -ForegroundColor Green
} else {
    Write-Host "✗ ОШИБКА: Файл поврежден или подделан!" -ForegroundColor Red
}
```

Эта команда автоматически прочитает официальную сумму, вычислит текущую и сравнит их.

---

## 📚 Примеры использования

### Пример 1: Разработчик создает релиз

```powershell
# 1. Собрать инсталлятор
.\build_windows.bat

# 2. Создать контрольную сумму
$Hash = (Get-FileHash "installer_output\VPN-Server-Manager-Setup-v4.0.7.exe" -Algorithm SHA256).Hash
$Hash | Out-File "installer_output\checksum.txt" -Encoding ASCII

# 3. Создать README для релиза
@"
# VPN Server Manager v4.0.7

## Download
- [VPN-Server-Manager-Setup-v4.0.7.exe](link)

## Verification
SHA-256: $Hash

## How to verify:
``````powershell
Get-FileHash "VPN-Server-Manager-Setup-v4.0.7.exe" -Algorithm SHA256
``````
"@ | Out-File "installer_output\RELEASE_NOTES.md"

# 4. Загрузить на GitHub Releases
Write-Host "Upload these files to GitHub:"
Write-Host "- VPN-Server-Manager-Setup-v4.0.7.exe"
Write-Host "- checksum.txt"
Write-Host "- RELEASE_NOTES.md"
```

### Пример 2: Пользователь проверяет загруженный файл

```powershell
# Скачали installer и checksum.txt

# Прочитать официальную сумму
$Official = Get-Content "checksum.txt" -Raw
$Official = $Official.Trim()

# Вычислить сумму загруженного файла
$Downloaded = (Get-FileHash "VPN-Server-Manager-Setup-v4.0.7.exe" -Algorithm SHA256).Hash

# Сравнить
if ($Official -eq $Downloaded) {
    Write-Host "✓ Файл подлинный, можно устанавливать!" -ForegroundColor Green
} else {
    Write-Host "✗ НЕ УСТАНАВЛИВАЙТЕ! Файл поврежден или подделан!" -ForegroundColor Red
}
```

### Пример 3: Автоматическая проверка при установке

В `vpn-manager-installer.iss` можно добавить проверку:

```pascal
[Code]
function VerifyInstallerIntegrity: Boolean;
var
  ExpectedHash: String;
  ActualHash: String;
  ResultCode: Integer;
begin
  ExpectedHash := '5CCD6B7C30F34C8715BFA54561F9E2A0B6B90EB3AC6B4AB491CCAB0396989B11';
  
  // Вычислить SHA-256 текущего файла
  Exec('powershell', 
    '-Command "Get-FileHash ''' + ExpandConstant('{srcexe}') + ''' -Algorithm SHA256"',
    '', SW_HIDE, ewWaitUntilTerminated, ResultCode);
  
  // Сравнить...
  Result := (ActualHash = ExpectedHash);
end;
```

---

## 🤖 Автоматизация

### Готовый скрипт проверки (BAT)

**`verify_installer.bat` — для пользователей:**

```batch
@echo off
REM Автоматическая проверка контрольной суммы

set INSTALLER=VPN-Server-Manager-Setup-v4.0.7.exe
set CHECKSUM_FILE=checksum.txt

REM Проверка наличия файлов
if not exist "%INSTALLER%" (
    echo [ОШИБКА] Файл %INSTALLER% не найден!
    pause
    exit /b 1
)

if not exist "%CHECKSUM_FILE%" (
    echo [ОШИБКА] Файл %CHECKSUM_FILE% не найден!
    pause
    exit /b 1
)

echo [OK] Все файлы найдены

REM Читаем официальную сумму
set /p EXPECTED=<%CHECKSUM_FILE%
echo Официальная сумма: %EXPECTED%

REM Вычисляем текущую сумму через certutil
certutil -hashfile "%INSTALLER%" SHA256 > temp_hash.txt 2>&1

REM Извлекаем хеш (вторая строка)
set LINE_NUM=0
for /f "skip=1 tokens=*" %%a in (temp_hash.txt) do (
    if !LINE_NUM!==0 (
        set ACTUAL=%%a
        set LINE_NUM=1
    )
)
del temp_hash.txt 2>nul

REM Удаляем пробелы
set ACTUAL=%ACTUAL: =%
echo Вычисленная сумма: %ACTUAL%

REM Сравниваем
if /i "%EXPECTED%"=="%ACTUAL%" (
    echo ========================================
    echo [OK] ПРОВЕРКА УСПЕШНА!
    echo ========================================
    echo Файл подлинный и не поврежден.
    
    choice /C YN /M "Запустить установку"
    if errorlevel 1 start "" "%INSTALLER%"
) else (
    echo ========================================
    echo [ERROR] ПРОВЕРКА НЕ ПРОЙДЕНА!
    echo ========================================
    echo ВНИМАНИЕ: Файл поврежден или подделан!
    echo НЕ УСТАНАВЛИВАЙТЕ ЭТОТ ФАЙЛ!
    pause
    exit /b 1
)
```

**Преимущества этого скрипта:**
- ✅ Полностью автоматический
- ✅ Проверяет наличие всех файлов
- ✅ Извлекает хеш правильно (убирает пробелы)
- ✅ Сравнивает без учета регистра
- ✅ Предлагает запустить установку
- ✅ Работает на любой Windows

### Готовый скрипт проверки (PowerShell)

**`verify_installer.ps1` — расширенная версия:**

```powershell
# Автоматическая проверка с подробным выводом

$InstallerFile = "VPN-Server-Manager-Setup-v4.0.7.exe"
$ChecksumFile = "checksum.txt"

Write-Host "========================================" -ForegroundColor Cyan
Write-Host "Проверка подлинности инсталлятора" -ForegroundColor Cyan
Write-Host "========================================" -ForegroundColor Cyan
Write-Host ""

# Проверка файлов
if (-not (Test-Path $InstallerFile)) {
    Write-Host "ОШИБКА: $InstallerFile не найден!" -ForegroundColor Red
    exit 1
}

if (-not (Test-Path $ChecksumFile)) {
    Write-Host "ОШИБКА: $ChecksumFile не найден!" -ForegroundColor Red
    exit 1
}

Write-Host "✓ Все файлы найдены" -ForegroundColor Green
Write-Host ""

# Читаем официальную сумму
$ExpectedHash = Get-Content $ChecksumFile -Raw | ForEach-Object { $_.Trim() }
Write-Host "Официальная сумма:" -ForegroundColor Yellow
Write-Host "  $ExpectedHash" -ForegroundColor White
Write-Host ""

# Вычисляем текущую сумму
Write-Host "Вычисление контрольной суммы..." -ForegroundColor Yellow
Write-Host "(это может занять несколько секунд)" -ForegroundColor Gray
$ActualHash = (Get-FileHash $InstallerFile -Algorithm SHA256).Hash
Write-Host "Вычисленная сумма:" -ForegroundColor Yellow
Write-Host "  $ActualHash" -ForegroundColor White
Write-Host ""

# Сравниваем
if ($ExpectedHash -eq $ActualHash) {
    Write-Host "========================================" -ForegroundColor Green
    Write-Host "✓ ПРОВЕРКА УСПЕШНА!" -ForegroundColor Green
    Write-Host "========================================" -ForegroundColor Green
    Write-Host ""
    Write-Host "Файл подлинный и не поврежден." -ForegroundColor Green
    Write-Host "Можно безопасно устанавливать." -ForegroundColor Green
    Write-Host ""
    
    # Показываем информацию о файле
    $FileInfo = Get-Item $InstallerFile
    Write-Host "Информация о файле:" -ForegroundColor Cyan
    Write-Host "  Имя:    $($FileInfo.Name)" -ForegroundColor White
    Write-Host "  Размер: $([math]::Round($FileInfo.Length / 1MB, 2)) МБ" -ForegroundColor White
    Write-Host "  Дата:   $($FileInfo.LastWriteTime)" -ForegroundColor White
    Write-Host ""
    
    # Предлагаем запустить
    $Response = Read-Host "Запустить установку сейчас? (Y/N)"
    if ($Response -eq 'Y' -or $Response -eq 'y') {
        Write-Host "Запуск инсталлятора..." -ForegroundColor Cyan
        Start-Process -FilePath $InstallerFile
    }
} else {
    Write-Host "========================================" -ForegroundColor Red
    Write-Host "✗ ПРОВЕРКА НЕ ПРОЙДЕНА!" -ForegroundColor Red
    Write-Host "========================================" -ForegroundColor Red
    Write-Host ""
    Write-Host "ВНИМАНИЕ: Файл поврежден или подделан!" -ForegroundColor Red
    Write-Host "НЕ УСТАНАВЛИВАЙТЕ ЭТОТ ФАЙЛ!" -ForegroundColor Red
    Write-Host ""
    Write-Host "Рекомендации:" -ForegroundColor Cyan
    Write-Host "  1. Удалите этот файл" -ForegroundColor White
    Write-Host "  2. Очистите кэш браузера" -ForegroundColor White
    Write-Host "  3. Скачайте заново с официального сайта" -ForegroundColor White
    Write-Host "  4. Проверьте снова" -ForegroundColor White
}
```

**Преимущества PowerShell версии:**
- ✅ Цветной вывод для наглядности
- ✅ Подробная информация о файле
- ✅ Детальные сообщения об ошибках
- ✅ Автоматический запуск установки
- ✅ Удобный интерактивный интерфейс

### Скрипт для массовой генерации контрольных сумм

**`generate_checksums.ps1`:**

```powershell
param(
    [string]$Directory = "installer_output"
)

Write-Host "Generating checksums for files in: $Directory" -ForegroundColor Cyan
Write-Host ""

$Files = Get-ChildItem -Path $Directory -File -Filter "*.exe"

foreach ($File in $Files) {
    Write-Host "Processing: $($File.Name)" -ForegroundColor Yellow
    
    $Hash = (Get-FileHash $File.FullName -Algorithm SHA256).Hash
    $OutputFile = "$($File.DirectoryName)\$($File.BaseName)_SHA256.txt"
    
    # Сохранить сумму
    $Hash | Out-File -FilePath $OutputFile -Encoding ASCII
    
    # Вывести информацию
    Write-Host "  SHA-256: $Hash" -ForegroundColor Green
    Write-Host "  Saved to: $OutputFile" -ForegroundColor Gray
    Write-Host ""
}

Write-Host "Done!" -ForegroundColor Green
```

**Использование:**
```powershell
.\generate_checksums.ps1
.\generate_checksums.ps1 -Directory "C:\Releases"
```

### Интеграция в build_windows.bat

Уже реализовано в нашем скрипте:

```batch
REM Создаем контрольную сумму SHA-256
echo Creating SHA-256 checksum...
powershell -Command "Get-FileHash '%OUTPUT_FILE%' -Algorithm SHA256 | Select-Object -ExpandProperty Hash" > installer_output\checksum.txt

if exist "installer_output\checksum.txt" (
    echo.
    echo SHA-256:
    type installer_output\checksum.txt
    echo.
)
```

---

## 🛡️ Безопасность

### ✅ Хорошие практики:

1. **Публикуйте контрольные суммы отдельно** от инсталлятора
2. **Используйте HTTPS** для загрузки обоих файлов
3. **Размещайте на официальном сайте/GitHub**
4. **Подписывайте релизы** GPG ключом (дополнительно)
5. **Документируйте процесс проверки** для пользователей

### ❌ Плохие практики:

1. ❌ Хранить сумму внутри инсталлятора (бесполезно)
2. ❌ Публиковать только на сторонних сайтах
3. ❌ Не обновлять суммы при новых версиях
4. ❌ Использовать устаревшие алгоритмы (MD5, SHA1)

---

## 📖 Для пользователей

### Зачем проверять контрольную сумму?

**Сценарий 1: Защита от вирусов**
```
Вы скачали файл с неизвестного сайта
→ Проверили SHA-256
→ Не совпала с официальной
→ Не устанавливали
→ Избежали заражения
```

**Сценарий 2: Поврежденная загрузка**
```
Интернет оборвался во время загрузки
→ Файл скачался не полностью
→ SHA-256 не совпала
→ Скачали заново
→ Установка прошла успешно
```

### Как проверить (для начинающих)?

**Шаг 1:** Откройте PowerShell (правой кнопкой Пуск → Windows PowerShell)

**Шаг 2:** Перейдите в папку с файлом:
```powershell
cd "C:\Users\ВашеИмя\Downloads"
```

**Шаг 3:** Запустите проверку:
```powershell
Get-FileHash "VPN-Server-Manager-Setup-v4.0.7.exe" -Algorithm SHA256
```

**Шаг 4:** Сравните результат с `checksum.txt` или README

**Шаг 5:** Если совпадает — устанавливайте! Если нет — не рискуйте!

### ❌ Что делать, если контрольные суммы НЕ совпали?

**ВНИМАНИЕ: НЕ УСТАНАВЛИВАЙТЕ ФАЙЛ!**

Если контрольная сумма не совпала, выполните следующие действия:

1. **Удалите загруженный файл**
   ```powershell
   Remove-Item "VPN-Server-Manager-Setup-v4.0.7.exe"
   ```

2. **Очистите кэш браузера**
   - Chrome: Ctrl+Shift+Del → "Кэшированные файлы"
   - Firefox: Ctrl+Shift+Del → "Кэш"
   - Edge: Ctrl+Shift+Del → "Кэшированные данные"

3. **Скачайте файл заново** с официального источника:
   - GitHub Releases: https://github.com/kureinmaxim/vpn-server-manager/releases
   - Только с официальной страницы!

4. **Проверьте контрольную сумму снова**

5. **Если проблема повторяется:**
   - Проверьте антивирус (может изменять файлы)
   - Попробуйте другой браузер
   - Проверьте подключение к интернету
   - Создайте Issue на GitHub с описанием проблемы

---

## 🔗 Дополнительные ресурсы

### Официальная документация:

- [Microsoft: Get-FileHash](https://docs.microsoft.com/en-us/powershell/module/microsoft.powershell.utility/get-filehash)
- [CertUtil Documentation](https://docs.microsoft.com/en-us/windows-server/administration/windows-commands/certutil)
- [SHA-256 на Wikipedia](https://en.wikipedia.org/wiki/SHA-2)

### Инструменты:

- **HashTab** — GUI расширение для Windows Explorer
- **7-Zip** — встроенная проверка контрольных сумм (CRC)
- **HashCheck** — расширение Shell для Windows

### Онлайн калькуляторы (НЕ рекомендуется для чувствительных файлов):

- https://emn178.github.io/online-tools/sha256_checksum.html
- https://md5file.com/calculator

⚠️ **Внимание:** Не загружайте приватные или исполняемые файлы на онлайн сервисы!

---

## 📝 Чек-лист для релиза

### Для разработчика:

При создании нового релиза:

- [ ] Собрать инсталлятор (`build_windows.bat` или `build_windows.ps1`)
- [ ] Создать SHA-256 контрольную сумму (автоматически при сборке)
- [ ] Сохранить в `checksum.txt` ✓ (автоматически)
- [ ] Создать `verify_installer.ps1` и `verify_installer.bat`
- [ ] Создать `HOW_TO_VERIFY.txt` с инструкциями
- [ ] Добавить сумму в `RELEASE_INFO.txt` ✓ (автоматически)
- [ ] Добавить сумму в README релиза на GitHub
- [ ] Загрузить все файлы на GitHub Releases:
  - [ ] `VPN-Server-Manager-Setup-v4.0.7.exe`
  - [ ] `checksum.txt`
  - [ ] `verify_installer.ps1`
  - [ ] `verify_installer.bat`
  - [ ] `HOW_TO_VERIFY.txt`
  - [ ] `RELEASE_INFO.txt`
  - [ ] `README.md` (из installer_output)
- [ ] Обновить документацию с инструкциями по проверке
- [ ] Обновить CHANGELOG.md
- [ ] Анонсировать релиз с указанием контрольной суммы
- [ ] Протестировать скрипты проверки
- [ ] Проверить инсталлятор на чистой системе

### Для пользователя:

При скачивании релиза:

- [ ] Скачать инсталлятор с официального GitHub Releases
- [ ] Скачать `checksum.txt` или `verify_installer.ps1`/`.bat`
- [ ] Запустить скрипт проверки ИЛИ проверить вручную
- [ ] Убедиться, что контрольные суммы совпадают
- [ ] Только после проверки запускать установку
- [ ] Сообщить о проблемах, если сумма не совпала

---

## ❓ FAQ

**Q: Контрольная сумма изменилась после пересборки. Это нормально?**  
A: Да, если вы внесли любые изменения в исходники или пересобрали с другими настройками.

**Q: Можно ли использовать MD5 вместо SHA-256?**  
A: Нет, MD5 устарел и небезопасен. Используйте только SHA-256 или выше.

**Q: Нужно ли проверять суммы файлов от официальных источников?**  
A: Да! Даже официальные сайты могут быть взломаны.

**Q: Что делать, если сумма не совпала?**  
A: НЕ УСТАНАВЛИВАЙТЕ! Скачайте заново с официального источника. См. раздел ["Что делать, если контрольные суммы НЕ совпали?"](#-что-делать-если-контрольные-суммы-не-совпали)

**Q: Как часто нужно создавать новые суммы?**  
A: При каждом новом релизе или изменении файла.

**Q: Где взять готовые скрипты проверки?**  
A: Они включены в релиз на GitHub: `verify_installer.ps1` и `verify_installer.bat`. Также можно создать самостоятельно по примерам из этого руководства.

**Q: Нужно ли проверять контрольную сумму каждый раз при запуске?**  
A: Нет, достаточно один раз при скачивании. После установки файл уже на вашем компьютере.

**Q: Certutil показывает сумму с пробелами. Это нормально?**  
A: Да, certutil добавляет пробелы между байтами. Наши скрипты автоматически их убирают. При ручном сравнении игнорируйте пробелы.

**Q: Почему контрольная сумма такая длинная?**  
A: SHA-256 создает 256-битный хеш (64 символа в hex). Это обеспечивает высокую надежность.

**Q: Можно ли проверить сумму до полной загрузки файла?**  
A: Нет, для вычисления SHA-256 нужен весь файл целиком.

**Q: Антивирус изменил файл. Что делать?**  
A: Временно отключите антивирус, скачайте заново, проверьте сумму ДО включения антивируса. Затем добавьте файл в исключения.

**Q: Где найти официальную контрольную сумму?**  
A: В файле `checksum.txt` на GitHub Releases, в `RELEASE_INFO.txt` или в описании релиза на GitHub.

---

**VPN Server Manager v4.0.7** | [GitHub](https://github.com/kureinmaxim/vpn-server-manager)

*Создано автоматически при сборке инсталлятора*

