# Архитектурные правила для Flask-приложения v4.0.3

## Контекст проекта

**VPN Server Manager** - Flask-приложение с desktop GUI (pywebview), поддержкой интернационализации, SSH/SFTP функциональностью и криптографией.

**v4.0.3**: 
- ✅ Централизованное управление версией из `config.json`
- ✅ Multi-App Support (параллельный запуск)
- ✅ Модульная архитектура (Application Factory, Service Layer)
- ✅ DataManagerService для управления данными
- ✅ Корректная работа в frozen режиме (macOS .app)
- ✅ Правильные пути для логов/данных в Application Support
- ✅ Desktop launcher (`launch_gui.py`) для запуска из Finder

## Структура проекта (v4.0.3)

```
VPNserverManage-Clean/
├── run.py                        # Точка входа (web/desktop режимы)
├── launch_gui.py                 # 🆕 Desktop launcher для Finder (v4.0.3)
├── run_desktop.py                # Обёртка для --desktop режима
├── config.json                   # 🎯 Конфигурация (version: 4.0.3)
├── .env                          # Секреты (SECRET_KEY)
├── .env.example
├── .gitignore
├── requirements.txt
├── setup.py                      # Автоматически читает версию из config.json
├── build_macos.py                # Сборка с версией из config.json
├── Info.plist.template           # 🆕 Шаблон Info.plist с NSPrincipalClass
├── Makefile                      # Команды разработки
├── babel.cfg                     # Babel конфигурация
│
├── app/                          # Основное приложение
│   ├── __init__.py              # Application Factory + load_app_info
│   ├── config.py                # Конфигурация (APP_DATA_DIR, APP_VERSION)
│   ├── exceptions.py            # Кастомные исключения
│   ├── models/                  # Модели данных
│   │   ├── __init__.py
│   │   └── server.py           # Модель VPN сервера
│   ├── services/                # Бизнес-логика (Service Layer)
│   │   ├── __init__.py         # ServiceRegistry (Dependency Injection)
│   │   ├── ssh_service.py      # SSH/SFTP операции
│   │   ├── crypto_service.py   # Шифрование/дешифрование
│   │   ├── api_service.py      # HTTP API запросы
│   │   └── data_manager_service.py  # 🆕 Управление данными (v4.0.1+)
│   ├── routes/                  # Маршруты (Blueprint Architecture)
│   │   ├── __init__.py
│   │   ├── main.py             # Основные роуты + /shutdown (v4.0.2)
│   │   └── api.py              # API endpoints + PIN auth
│   └── utils/                   # Утилиты
│       ├── __init__.py
│       ├── validators.py
│       └── decorators.py       # @require_auth, @require_pin
│
├── desktop/                     # Desktop GUI слой
│   ├── __init__.py
│   └── window.py               # 🆕 WSGI + динамические порты (v4.0.2)
│
├── templates/                   # Jinja2 шаблоны (вне app/)
│   ├── layout.html
│   ├── index.html
│   ├── index_locked.html       # PIN вход
│   ├── settings.html
│   └── ...
│
├── static/                      # Статические файлы (вне app/)
│   ├── css/
│   ├── js/
│   ├── images/
│   └── fonts/
│
├── translations/                # Flask-Babel переводы (вне app/)
│   ├── en/LC_MESSAGES/
│   ├── ru/LC_MESSAGES/
│   └── zh/LC_MESSAGES/
│
├── data/                        # Данные приложения
│   ├── servers.json.enc        # Зашифрованные серверы
│   └── merged_*.enc            # Импортированные данные
│
├── uploads/                     # Загруженные иконки серверов
├── logs/                        # Логи приложения
│   └── app.log
│
├── tests/                       # Тесты
│   ├── __init__.py
│   ├── conftest.py
│   ├── test_services/
│   └── test_routes/
│
├── docs/                        # Документация
│   ├── project_info/
│   │   ├── PROJECT_STRUCTURE.md
│   │   ├── BUILD.md
│   │   ├── BACKUP_TOOLS.md
│   │   └── SECRET_KEY.md
│   ├── release_guide.md
│   └── github_push_guide.md
│
└── backup_tools/                # Инструменты резервного копирования
    └── ...
```

## Контрольный список (Checklist v4.0.3)

### Архитектура
- [ ] Application Factory реализован
- [ ] Версия загружается из `config.json`
- [ ] Все секреты в `.env`
- [ ] Blueprints для модульности (main, api, pin)
- [ ] Service Layer для бизнес-логики
- [ ] ServiceRegistry для DI
- [ ] Кастомные исключения и обработчики

### Безопасность
- [ ] Валидация входных данных
- [ ] SECRET_KEY в `.env` (Fernet-совместимый)
- [ ] PIN-аутентификация реализована
- [ ] Уникальные cookie-сессии (`SESSION_COOKIE_NAME`)
- [ ] Данные шифруются (Fernet)

### i18n и UI
- [ ] Flask-Babel настроен (ru, en, zh)
- [ ] Все тексты через `gettext`
- [ ] Контекстный процессор `app_info`

### Desktop & Multi-App
- [ ] WSGI сервер с портом 0
- [ ] Глобальные переменные `SERVER_PORT`
- [ ] Graceful Shutdown реализован
- [ ] `/shutdown` эндпоинт

### Качество кода
- [ ] Логирование настроено
- [ ] Тесты написаны
- [ ] Type hints используются
- [ ] Docstrings для публичных методов
- [ ] PEP 8 соблюдается

### Документация
- [ ] README актуален
- [ ] CHANGELOG обновлен
- [ ] ArchitecturalRules актуален
- [ ] BUILD.md актуален

### Git
- [ ] `.gitignore` содержит `.env`, `__pycache__`, etc.
- [ ] `requirements.txt` актуален
- [ ] Коммиты следуют [Conventional Commits](https://www.conventionalcommits.org/)

## Команды для работы (v4.0.3)

### Makefile команды
```bash
# Версия
make version                  # Показать версию из config.json

# Запуск
make run                      # Web режим (динамический порт)
make run-desktop              # Desktop режим (WSGI с портом 0)
make run-debug                # Debug режим

# Разработка
make install-dev              # Установить dev зависимости
make test                     # Запустить тесты
make lint                     # Проверить код
make format                   # Форматировать код
make all                      # Все проверки

# Сборка
make build                    # Собрать .app
make dist                     # Собрать .app + .dmg
make clean                    # Очистить временные файлы

# Docker
make docker-build             # Собрать образ
make docker-run               # Запустить (порт 5000)
make docker-run-multi         # 3 контейнера (5000-5002)
make docker-stop-multi        # Остановить все

# i18n
make init-translations        # Инициализировать переводы
make update-translations      # Обновить переводы
make compile-translations     # Скомпилировать переводы
```

### Ручные команды
```bash
# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate  # macOS/Linux
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt

# Проверка версии
make version
# Вывод: VPN Server Manager v4.0.3

# Запуск тестов
pytest
pytest --cov=app          # С покрытием

# Запуск приложения
python run.py              # Web режим
python run.py --desktop    # Desktop режим
python run.py --debug      # Debug режим
```

## Примечания (v4.0.3)

### Версионирование
- **Версия хранится ТОЛЬКО в `config.json`**
- Все компоненты читают её автоматически
- Для изменения версии: измените `config.json` → `app_info.version`

### Multi-App Support
- Desktop: порт 0 → ОС выбирает свободный
- Web: `find_free_port()` начиная с 5000
- Cookie: `vpn_manager_session_clean` (уникальное имя)
- Shutdown: `/shutdown` эндпоинт + `on_closing` обработчик

### Best Practices
- Всегда следуйте PEP 8
- Используйте type hints (Python 3.10+)
- Документируйте публичные методы docstrings
- Версионируйте API endpoints
- Регулярно обновляйте зависимости (`pip list --outdated`)
- Делайте бэкапы `.env` файла (SECRET_KEY)!

### Данные
- **Production (macOS frozen)**:
  - Данные: `~/Library/Application Support/VPNServerManager-Clean/`
  - Логи: `~/Library/Logs/VPNServerManager/app.log`
  - Загрузки: `~/Library/Application Support/VPNServerManager-Clean/uploads/`
- **Development**: директория проекта
- **Конфигурация**: `config.json` (версия 4.0.3)
- **Секреты**: `.env` (SECRET_KEY)
- **Данные**: `data/servers.json.enc` (зашифровано)

### Запуск из Finder (macOS)
- **Launcher**: `launch_gui.py` (точка входа PyInstaller)
- **Логирование**: Автоматическое перенаправление в `~/Library/Logs/VPNServerManager/app.log`
- **Frozen detection**: `sys.frozen` для определения режима
- **Иконка**: `icon_clean.icns` (конвертирован из PNG с размерами 16x16 → 1024x1024)

---

**Документ создан для v4.0.3** - централизованная версия, Multi-App Support, модульная архитектура.

Полный текст архитектурных правил см. в основном файле `ArchitecturalRules.md`.

